import { ElementRef, OnDestroy, OnInit } from '@angular/core';
import { Subscription } from 'rxjs';
import { IDisplayDensityOptions, DisplayDensityBase } from '../../core/displayDensity';
import { IgxIconService } from '../../icon/public_api';
import { IgxGridToolbarTitleDirective, IgxGridToolbarActionsDirective } from './common';
import { GridBaseAPIService } from '../api.service';
import { IgxGridBaseDirective } from '../grid-base.directive';
import { GridType } from '../common/grid.interface';
/**
 * Provides a context-aware container component for UI operations for the grid components.
 *
 * @igxModule IgxGridToolbarModule
 *
 */
import * as ɵngcc0 from '@angular/core';
export declare class IgxGridToolbarComponent extends DisplayDensityBase implements OnInit, OnDestroy {
    protected _displayDensityOptions: IDisplayDensityOptions;
    private api;
    private iconService;
    private element;
    /**
     * When enabled, shows the indeterminate progress bar.
     *
     * @remarks
     * By default this will be toggled, when the default exporter component is present
     * and an exporting is in progress.
     */
    showProgress: boolean;
    /**
     * Gets/sets the grid component for the toolbar component.
     *
     * @remarks
     * Usually you should not set this property in the context of the default grid/tree grid.
     * The only grids that demands this to be set are the hierarchical child grids. For additional
     * information check the toolbar topic.
     */
    get grid(): IgxGridBaseDirective;
    set grid(value: IgxGridBaseDirective);
    /** Returns the native DOM element of the toolbar component */
    get nativeElement(): HTMLElement;
    /**
     * @hidden
     * @internal
     */
    hasTitle: IgxGridToolbarTitleDirective;
    /**
     * @hidden
     * @internal
     */
    hasActions: IgxGridToolbarActionsDirective;
    /**
     * @hidden
     * @internal
     */
    defaultStyle: boolean;
    /**
     * @hidden
     * @internal
     */
    get cosyStyle(): boolean;
    /**
     * @hidden
     * @internal
     */
    get compactStyle(): boolean;
    /** @hidden @internal */
    width: any;
    protected _grid: IgxGridBaseDirective;
    protected sub: Subscription;
    constructor(_displayDensityOptions: IDisplayDensityOptions, api: GridBaseAPIService<IgxGridBaseDirective & GridType>, iconService: IgxIconService, element: ElementRef<HTMLElement>);
    /** @hidden @internal */
    ngOnInit(): void;
    /** @hidden @internal */
    ngOnDestroy(): void;
    static ɵfac: ɵngcc0.ɵɵFactoryDeclaration<IgxGridToolbarComponent, [{ optional: true; }, null, null, null]>;
    static ɵcmp: ɵngcc0.ɵɵComponentDeclaration<IgxGridToolbarComponent, "igx-grid-toolbar", never, { "showProgress": "showProgress"; "grid": "grid"; }, {}, ["hasTitle", "hasActions"], ["[igxGridToolbarTitle],igx-grid-toolbar-title", "*", "[igxGridToolbarActions],igx-grid-toolbar-actions"]>;
}

//# sourceMappingURL=grid-toolbar.component.d.ts.map